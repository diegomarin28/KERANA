export default function SignIn() { //SIN SUPABASE
    return (
        <section style={{ padding: 20 }}>
            <h2>Crear cuenta</h2>
            <p>Página de registro (placeholder). Más adelante agregamos el formulario real.</p>
        </section>
    );
}
